import * as actionTypes from './types';

export const subscribeToLoop = () => ({ type: actionTypes.SUBSCRIBE_TO_LOOP });
export const unsubscribeFromLoop = () => ({ type: actionTypes.UNSUBSCRIBE_FROM_LOOP });
